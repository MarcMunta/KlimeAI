Prefer changes that are easy to validate.

Always include a short **Validation** section with exact commands (e.g. `pytest -q`, `npm run build`).
If you cannot run tests, say so explicitly and still provide the best next commands to run locally.

